# Vertex AI Web Tester (.env.example)
# 이 파일을 복사하여 .env 로 이름 변경 후 값을 채워주세요.

# GCP 프로젝트 ID (gcloud config get-value project 로 확인 가능)
PROJECT_ID=your-gcp-project-id

# Vertex AI 리전 (공식 문서에 따르면 gemini-2.5-flash / flash-image-preview 모두 Global/또는 us-central1 등에서 사용 가능)
# 일반적으로 us-central1 권장
VERTEX_LOCATION=us-central1

# 선택: 모델 ID를 오버라이드하려면 설정 (미설정 시 서버 기본값 사용)
# 텍스트/멀티모달 (GA)
VERTEX_TEXT_MODEL=gemini-2.5-flash
# 이미지 생성/편집 (Preview, aka nano-banana)
VERTEX_IMAGE_MODEL=gemini-2.5-flash-image-preview

# 선택: 서버 포트 (기본 3000)
# PORT=3000

# 인증 (ADC: Application Default Credentials)
# 권장: gcloud CLI를 이용한 사용자 인증 (로컬 개발)
# 1) gcloud auth application-default login
# 2) gcloud config set project YOUR_PROJECT_ID
# 필요시 서비스 계정 키를 사용하려면 아래 환경변수 사용:
# GOOGLE_APPLICATION_CREDENTIALS=C:\path\to\service-account.json
